# `docs/handoff/`

세션 간 핸드오프용 일회성 메모.

- 진행 중 작업 상태, WIP 스냅샷, 다음 세션 재개 시 필요한 컨텍스트만 둔다.
- 작업이 commit·머지되면 해당 파일은 **삭제하거나 archive 한다** (영구 문서 아님).
- 영구화해야 할 결정·룰은 `../architecture/v1/` 또는 `CLAUDE.md` 로 승격한다.

`architecture/v1/` (영구 설계) 와 `spec/` (제품 요구사항) 과 시각적으로 분리해
"살아있는 결정 vs 임시 메모" 혼동을 차단한다.
