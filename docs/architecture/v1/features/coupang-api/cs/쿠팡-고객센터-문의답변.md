# 쿠팡 고객센터 문의답변

> 출처: <https://developers.coupangcorp.com/hc/ko/articles/360034156233>
> Zendesk article id: `360034156233`
> 섹션: CS APIs
> 추출 시점: 2026-05-28

---

**API** **적용** **가능한** **구매자** **사용자** **지역****:** **한국****,** **대만**

판매자가 쿠팡 고객센터를 통해 접수된 문의에 대해 답변합니다.  
문의의 상태가 '미답변(inquiryStatus:progress, partnerTransferStatus:requestAnswer)'인 경우에만 답변 작성 가능합니다.  
고객센터 문의조회 API를 통해 해당 inquiryId, answerId를 확인한 뒤 답변 작성 부탁드립니다.

동일한 inquiry에 대해 중복해서 답변을 보내면 에러가 발생합니다.

※ 문의의 상태가 24시간 이상 ‘미답변’ 상태로 남아있는 경우,  
쿠팡 시스템에서 자동으로 “[쿠팡 시스템] CS문의가 24시간 내 답변되지 않아, 쿠팡 약관 및 정책에 따라 상담사가 처리할 예정임을 알려드립니다.” 안내가 문의에 달리게 됩니다.  
이 경우 안내와 동시에 문의 상태는 “답변완료“로 변경이 되므로 API를 통해 답변을 할 수 없으며  
필요 시 [WING > 온라인 문의 > 쿠팡CS문의] 페이지를 통해 답변 작성 가능합니다.

## Path

POST

/v2/providers/openapi/apis/api/v4/vendors/{vendorId}/callCenterInquiries/{inquiryId}/replies

### Example Endpoint

https://api-gateway.coupang.com/v2/providers/openapi/apis/api/v4/vendors/A00010028/callCenterInquiries/1007837444/replies

## Request Parameters

### Path Segment Parameter

| Name | | | | | Required | Type | Description |
| --- | --- | --- | --- | --- | --- | --- | --- |
| vendorId | | | | | O | String | 판매자 ID  쿠팡에서 업체에게 발급한 고유 코드. Wing 로그인 후 확인 가능 |
| inquiryId | | | | | O | String | 상담 번호 (질문 ID)  판매자가 어느 질문을 대답해야할지를 입력합니다. 먼저 쿠팡 고객센터 문의조회 API를 이용하여 inquiryId를 확인합니다. |

### Body Parameter

| Name | | | | | Required | Type | Description |
| --- | --- | --- | --- | --- | --- | --- | --- |
| vendorId | | | | | O | String | 판매자 ID  쿠팡에서 업체에게 발급한 고유 코드. Wing 로그인 후 확인 가능 |
| inquiryId | | | | | O | String | Inquiry ID |
| content | | | | | O | String | 답변 내용  최소 2 ~ 최대 1,000자까지 입력 가능  줄넘김을 위해서는 \n를 사용하시기 바랍니다. |
| replyBy | | | | | O | String | 실사용자ID(쿠팡 Wing ID)  업체(Vendor)에 소속된 사용자ID |
| parentAnswerId | | | | | O | Number | 부모이관글 ID(answerId)  쿠팡 고객센터 문의 조회API에서 조회한 answerId 를 입력해야 됩니다. |

### Request Example

```
{
  "vendorId": "A00010028",
  "inquiryId": "1007837444",
  "content": "안녕하세요\n주문 확인 부탁드립니다.\n수고하세요.",
  "replyBy": "wanger",
  "parentAnswerId": "1023208324"
}
```

## Response Message

| Name | | | | | Type | Description |
| --- | --- | --- | --- | --- | --- | --- |
| code | | | | | Number | 서버 응답 코드  200/400/500 |
| message | | | | | String | 서버 응답 메세지  OK |

### Response Example

```
{
  "code": "200",
  "message": "OK"
}
```

### Error Spec

| HTTP 상태 코드(오류 유형) | 오류 메시지 | 해결 방법 |
| --- | --- | --- |
| 400 (요청변수확인) | Could not read JSON: Illegal unquoted character ((CTRL-CHAR, code 13)): has to be escaped using backslash to be included in string value... | 답변내용(content)을 JSON 형식에 맞게 작성했는지 확인합니다. newline, backslash, carriage return 등의 문자는 JSON 형태로 변환이 필요합니다. |
| 400 (요청변수확인) | The inquiry can't be answer. It can do only inquiryStatus:progress, partnerTransferStatus:requestAnswer | 답변을 작성하려는 문의의 상태가 '미답변(inquiryStatus:progress, partnerTransferStatus:requestAnswer)'인지 확인 합니다. 답변을 작성한 문의에 중복으로 답변을 보낼 경우 에러가 발생합니다. |
| 400 (요청변수확인) | The reply content length should be between 2 ~ 1000 | 작성한 답변의 길이가 2자 이상 ~ 1,000자 이하 인지 확인합니다. |
| 400 (요청변수확인) | The parentAnswerId is required. | 답변 작성 시 하는 부모이관글의 ID (parentAnswerId)를 입력했는지 확인합니다. |
| 400(요청변수확인) | userId가 올바르지 않습니다 - 520 | replyBy에 작성한 userId가 올바르지 않습니다. userId에 문제가 없는 경우 쿠팡 온라인 문의를 통해 확인합니다. |
| 400(요청변수확인) | userId가 올바르지 않습니다 - 521 | replyBy에 작성한 userId가 올바르지 않습니다. userId에 문제가 없는 경우 쿠팡 온라인 문의를 통해 확인합니다. |
| 400(요청변수확인) | 상담이 종료된 문의입니다 - 522 | 상담사에 의해 상담이 종료된 문의입니다. 답변을 작성할 수 없습니다. |

### URL API Name

UPDATE\_INQUIRY
