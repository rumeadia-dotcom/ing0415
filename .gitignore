# Dependencies
node_modules/
.pnpm-store/
pnpm-debug.log*

# Build
dist/
build/
*.tsbuildinfo
.turbo/
.cache/

# Env / secrets
.env
.env.local
.env.*.local
*.local

# Supabase local
apps/api/supabase/.branches/
apps/api/supabase/.temp/
# CLI 가 잘못된 CWD 에서 실행될 때 만드는 repo-root stray (canonical: apps/api/supabase)
/supabase/

# Test / coverage
coverage/
playwright-report/
test-results/
.vitest-cache/

# Sentry
.sentryclirc

# Packaging
*.tgz

# IDE / OS
.vscode/
.idea/
.DS_Store
*.log

# Claude Code local settings
.claude/settings.local.json
.claude/worktrees/
.claude/scheduled_tasks.lock

# MCP hosting client config — 평문 토큰 커밋 방지 (mcp-hosting.md §10).
# 예시는 infra/mcp-hosting/mcp.json.example 로 커밋.
.mcp.json

# Design canvas editor state + duplicate uploads (mirror of docs/design-renewal/s*.md)
docs/design-renewal/designFile/.design-canvas.state.json
docs/design-renewal/designFile/uploads/

# Manual verification artifacts (scripts/verify-mock-flow.mjs 출력)
verify-out/

# Manual verification artifacts (scripts/verify-mock-flow.mjs 출력)
verify-out/
