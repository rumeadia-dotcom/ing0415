# 로컬 dev 모드 (committed). vite default mode=development 에서 자동 로드.
# Supabase 프로젝트 URL 은 공개 정보 (anon key 는 별도 .env.development.local 에).
# 시크릿/anon key 는 apps/web/.env.development.local (gitignored) 에 분리.
VITE_SUPABASE_URL=https://eqoywqoalwkwbrdsulfl.supabase.co
